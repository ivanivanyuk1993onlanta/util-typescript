import { IsSelectedContinuous$PurePipe } from './is-selected-continuous$-pure.pipe';

describe('IsSelectedContinuous$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new IsSelectedContinuous$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
