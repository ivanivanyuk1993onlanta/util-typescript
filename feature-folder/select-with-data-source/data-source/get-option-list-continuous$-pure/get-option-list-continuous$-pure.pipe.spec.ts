import { GetOptionListContinuous$PurePipe } from './get-option-list-continuous$-pure.pipe';

describe('GetOptionListContinuous$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetOptionListContinuous$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
