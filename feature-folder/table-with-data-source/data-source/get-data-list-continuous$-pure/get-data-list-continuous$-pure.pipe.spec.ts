import { GetDataListContinuous$PurePipe } from './get-data-list-continuous$-pure.pipe';

describe('GetDataListContinuous$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDataListContinuous$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
