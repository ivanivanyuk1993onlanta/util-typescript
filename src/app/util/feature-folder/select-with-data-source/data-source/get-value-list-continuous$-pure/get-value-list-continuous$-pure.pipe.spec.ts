import { GetValueListContinuous$PurePipe } from './get-value-list-continuous$-pure.pipe';

describe('GetValueListContinuous$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetValueListContinuous$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
