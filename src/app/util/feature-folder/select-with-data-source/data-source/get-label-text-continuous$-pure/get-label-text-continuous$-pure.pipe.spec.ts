import { GetLabelTextContinuous$PurePipe } from './get-label-text-continuous$-pure.pipe';

describe('GetLabelTextContinuous$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetLabelTextContinuous$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
