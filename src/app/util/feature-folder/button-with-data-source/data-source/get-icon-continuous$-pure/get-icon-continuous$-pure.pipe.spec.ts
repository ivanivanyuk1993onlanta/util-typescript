import { GetIconContinuous$PurePipe } from './get-icon-continuous$-pure.pipe';

describe('GetIconContinuous$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetIconContinuous$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
