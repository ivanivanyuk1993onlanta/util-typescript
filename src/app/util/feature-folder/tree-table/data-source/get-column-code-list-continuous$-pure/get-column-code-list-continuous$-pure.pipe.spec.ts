import { GetColumnCodeListContinuous$PurePipe } from './get-column-code-list-continuous$-pure.pipe';

describe('GetColumnCodeListContinuous$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetColumnCodeListContinuous$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
