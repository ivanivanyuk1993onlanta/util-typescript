import { GetUrlPurePipe } from './get-url-pure.pipe';

describe('GetUrlPurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetUrlPurePipe();
    expect(pipe).toBeTruthy();
  });
});
