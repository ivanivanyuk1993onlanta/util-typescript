import { GetDisplayTextBsPurePipe } from './get-display-text-bs-pure.pipe';

describe('GetDisplayTextBsPurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDisplayTextBsPurePipe();
    expect(pipe).toBeTruthy();
  });
});
