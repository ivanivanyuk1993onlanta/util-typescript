import { GetUrl$PurePipe } from './get-url$-pure.pipe';

describe('GetUrl$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetUrl$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
