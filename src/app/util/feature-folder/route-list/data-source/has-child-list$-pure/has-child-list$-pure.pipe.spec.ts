import { HasChildList$PurePipe } from './has-child-list$-pure.pipe';

describe('HasChildList$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new HasChildList$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
