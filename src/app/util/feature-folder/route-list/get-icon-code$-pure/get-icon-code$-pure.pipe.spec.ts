import { GetIconCode$PurePipe } from './get-icon-code$-pure.pipe';

describe('GetIconCode$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetIconCode$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
