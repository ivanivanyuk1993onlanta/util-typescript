import { GetChildListPurePipe } from './get-child-list-pure.pipe';

describe('GetChildListPurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetChildListPurePipe();
    expect(pipe).toBeTruthy();
  });
});
