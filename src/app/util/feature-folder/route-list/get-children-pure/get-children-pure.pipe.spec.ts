import { GetChildrenPurePipe } from './get-children-pure.pipe';

describe('GetChildrenPurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetChildrenPurePipe();
    expect(pipe).toBeTruthy();
  });
});
