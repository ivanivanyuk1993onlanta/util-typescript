import { GetDisplayTextBS$PurePipe } from './get-display-text-bs$-pure.pipe';

describe('GetDisplayTextBS$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDisplayTextBS$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
