import { GetColumnHeaderTextContinuousPurePipe } from './get-column-header-text-continuous-pure.pipe';

describe('GetColumnHeaderTextContinuousPurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetColumnHeaderTextContinuousPurePipe();
    expect(pipe).toBeTruthy();
  });
});
