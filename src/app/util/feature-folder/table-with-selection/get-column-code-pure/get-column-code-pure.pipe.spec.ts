import { GetColumnCodePurePipe } from './get-column-code-pure.pipe';

describe('GetColumnCodePurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetColumnCodePurePipe();
    expect(pipe).toBeTruthy();
  });
});
