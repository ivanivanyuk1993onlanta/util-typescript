import { GetKey$PurePipe } from './get-key$-pure.pipe';

describe('GetKey$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetKey$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
