import { GetColumnTextContinuousPurePipe } from './get-column-text-continuous-pure.pipe';

describe('GetColumnTextContinuousPurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetColumnTextContinuousPurePipe();
    expect(pipe).toBeTruthy();
  });
});
