import { GetSideNavContainerNgClassContinuous$PurePipe } from './get-side-nav-container-ng-class-continuous$-pure.pipe';

describe('GetSideNavContainerNgClassContinuous$PurePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSideNavContainerNgClassContinuous$PurePipe();
    expect(pipe).toBeTruthy();
  });
});
